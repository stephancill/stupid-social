{
  "name": "stupid-social",
  "private": true,
  "type": "module",
  "scripts": {
    "worker:check": "tsc --noEmit",
    "worker:dry-run": "wrangler deploy --dry-run",
    "worker:deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "typescript": "^5.8.3",
    "wrangler": "^4.110.0"
  }
}
